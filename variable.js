// Имя (строка)
const userName = "Алексей";

// Возраст (число)
let userAge = 33;

// Есть ли опыт в программировании (булево)
let hasExperience = false;

// Текущий язык (null, потом мы его зададим)
let currentLanguage = null;

// Результат теста (undefined, ещё не вычислен)
let testResult;
